# The benchmark adapter imports models/unet, so install that first:
#   pip install -r models/unet/requirements.txt
# These are the extras the adapter itself needs on top of it.
numpy>=1.26
scipy>=1.11
torch>=2.0
pyyaml>=6.0
cfgrib>=0.9.14
eccodes>=1.6.0
