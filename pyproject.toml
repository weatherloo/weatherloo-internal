[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "weatherloo"
version = "0.1.0"
description = "HRRR post-processing experiments for Waterloo-region station-supervised temperature correction."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "matplotlib",
  "numpy",
  "pandas",
  "xarray",
  "torch",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

